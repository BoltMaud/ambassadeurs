$(document).ready(function(){
  // Init dropdown Select
  $('select').formSelect();
  // Init mobile side nav
  $('.sidenav').sidenav();
  // Init tabs
  $('.tabs').tabs();
  // Init parallax
  $('.parallax').parallax();

});